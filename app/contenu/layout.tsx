export const metadata = {
  title: 'LudoTool - Contenu',
  description: 'Impression du contenu',
};

export default function ContenuLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}