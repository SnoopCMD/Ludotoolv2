export const metadata = {
  title: 'LudoTool - Agenda',
  description: 'Planning de l\'équipe',
};

export default function AgendaLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}