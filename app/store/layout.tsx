export const metadata = {
  title: 'LudoTool - Store',
  description: 'Veille et commandes de jeux',
};

export default function StoreLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
