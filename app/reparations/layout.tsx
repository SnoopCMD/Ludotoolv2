export const metadata = {
  title: 'LudoTool - Réparations',
  description: 'Gestion des réparations',
};

export default function ReparationsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}