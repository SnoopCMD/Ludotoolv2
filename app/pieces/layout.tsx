export const metadata = {
  title: 'LudoTool - Pièces Manquantes',
  description: 'Gestion des pièces manquantes et orphelines',
};

export default function PiecesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}